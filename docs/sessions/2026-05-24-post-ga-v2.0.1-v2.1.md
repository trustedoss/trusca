# Session Handoff — 2026-05-23~24 — Post-GA — v2.0.1 공개 준비 + v2.1 (EPSS·자동마이그레이션·UI갭)

## 1. 무엇을 했나
- **문서 점검·정정**: 사용자/관리자 가이드 EN/KO 정합성 점검 → NOTICE text/markdown/html 포맷, 취약점 PDF(구현됨), ORT→scancode stale, gcp-deploy KO frontmatter 정정. 스크린샷 78건 무결성·해상도 확인(누락 0).
- **경쟁 OSS 갭 분석**(DT/SW360/ScanCode/ScanOSS/Syft·Grype/Trivy/OSV-Scanner/GUAC/Renovate) → **Post-GA 로드맵 수립**: `ROADMAP.md`(공개) + `docs/post-ga-roadmap.md`(상세). 4 마일스톤(v2.0.1 문서/웹·배포, v2.1 EPSS·VEX소비, v2.2 리메디에이션·정책, v2.3 공급망무결성). **스니펫 검출 제외(사용자 결정)**.
- **웹사이트/배포·설치 검토**: 랜딩 stale·social-card 누락 발견; 이미지 미게시(공개 차단) 발견. UI/UX 검토(화면 직접 확인) — 글로벌 SCA SaaS 수준 일관·심플 확인.
- **머지된 PR (전부 main)**:
  - #129 v2.0.1 공개 준비(README/랜딩 stale 정정, 거버넌스 GOVERNANCE/MAINTAINERS/SUPPORT/CODEOWNERS, comparison 페이지, social-card, **release.yml 멀티아치 ghcr 게시**, compose/env/helm ghcr 정렬, install.sh Compose V2 fallback, no-clone 설치 문서)
  - #131 typecheck 게이트 복구(tests.* mypy override 보강)
  - #130 backend entrypoint 자동 alembic(AUTO_MIGRATE, advisory lock, security 반영)
  - #132/#134 EPSS 1급화(모델 0015 + dt_resync 수집 + sort/min_epss/GATE_EPSS_THRESHOLD + UI 컬럼·필터·드로어 + EN/KO + 경계·E2E 테스트) + audit Decimal 직렬화 latent 버그 수정
  - #135 alembic head test robust화(`(head)` 마커)
  - #133 starlette 1.0.1 + fastapi 0.133.1 (PYSEC-2026-161)
  - #136 Policy Gate 판정 카드 + 컨테이너 스캔 kind/image_ref UI
  - #137 헤더 stale "v2 — Phase 0" 제거
  - #138 문서 싱크(Policy Gate UI·컨테이너 스캔 + stale 정정)

## 2. 결정 사항 / 변경된 가정
- **스니펫 검출(ScanOSS급) 범위 제외** — 사용자 결정. 향후 재검토 시 별도 RFC.
- **자체 취약점 DB 미구축** — DT 집계 유지, EPSS·VEX로 보완.
- **PR 코멘트 수동 게시 UI 비구현** — CI 자동 게시가 주 경로, 심플 UX 원칙상 덕지덕지 회피.
- **alembic head test**는 revision 하드코딩 대신 `(head)` 마커 확인(forward-only 유지, 마이그레이션마다 안 깨짐).
- **자율 머지 권한 부여**(사용자) — 단 public 이미지 게시(태그)는 여전히 사용자 작업.

## 3. 현재 상태
- `main` HEAD = `d4ed9bc` (#138). 미머지 PR 없음. main typecheck/test green(로컬 검증).
- v2.0.1 코드/문서 완료, **이미지 게시는 운영 단계 미완**(아래 블로커).
- v2.1: EPSS·자동마이그레이션·Policy Gate UI·컨테이너 스캔 완료. **VEX 소비·라이브 데모·API 호스팅·Helm 프로덕션화·/health/ready 미착수**.
- 구현↔UI 갭: Explore가 5건 "미반영"이라 했으나 4건(Backup·Audit CSV·scan취소·DT제어)은 **이미 구현**(오탐), PR 코멘트 1건만 진짜 갭(비구현 결정).

## 4. 다음 세션이 할 일 (우선순위)
1. **[운영·사용자] v2.0.1 이미지 게시** — 첫 `v2.0.1` 태그 cut → `trustedoss` org Actions packages:write + ghcr 패키지 public → `install-uat` `continue-on-error` 제거. **공개 차단점.**
2. **[v2.1] VEX 소비** — OpenVEX/CycloneDX VEX 임포트 → finding 자동 not_affected/suppressed(근거·감사로그). EPSS와 짝(노이즈 감소, DT 종속 완화 완결). `vuln_finding_status`·export 경로 재사용.
3. **[v2.1] 평가 경로** — 라이브 데모 + 경량 프로파일/시드, API 레퍼런스 호스팅(OpenAPI), Helm 프로덕션화(Ingress/TLS·OCI), `/health/ready`(자동마이그레이션 follow-up).
4. **[v2.2] 자동 리메디에이션** — 권장 업그레이드 → 옵트인 PR. 최대 전략 공백(Renovate/OSV-Scanner 영역).
5. **[follow-up] 스크린샷 갱신** — EPSS 컬럼·게이트 카드·컨테이너 다이얼로그 반영(EN/KO).

## 5. 주의 · 블로커
- **공개 차단**: 이미지 미게시 — 사용자 GitHub admin 작업 필요(코드 불가).
- **자율 머지 시 main CI(특히 integration/brittle test) 확인** — #132가 alembic head test 깨고 머지돼 main red 누적했던 사례. CI `could not read Username`은 checkout fluke(코드 무관, 재실행). → 메모리 [[feedback-autonomous-merge-ci-check]].
- **갭 분석 오탐** — Explore의 "UI 미반영"은 호출추적 한계로 오탐 多. frontend-dev 실검증 먼저. → [[feedback-explore-gap-false-positives]].
- pre-existing: `uq_components_purl` 컴포넌트 prefix 충돌(seed), 컴포넌트-모드 seed run-suffix 미적용.

## 6. 다음 세션 시작 지시문 (복붙용)
> TrustedOSS Portal post-GA 진행. 현재 main=#138(d4ed9bc), v2.0.1+v2.1 상당부분 머지 완료. `docs/post-ga-roadmap.md`가 단일 진실. 다음 작업으로 **v2.1 VEX 소비**(OpenVEX/CycloneDX VEX 임포트 → finding 자동 not_affected/suppressed, EPSS와 짝)를 Pipeline+Producer-Reviewer로 진행해줘. 먼저 `vuln_finding_status` enum·VEX export 경로·vulnerability_service를 확인하고, db-designer(필요시)→backend(임포트/매핑)→frontend(UI)→i18n/doc 순으로. 자율 머지 시 main CI(integration test) 확인 필수. 별도로 v2.0.1 이미지 게시(태그+ghcr org 설정)는 사용자 운영 작업으로 대기 중.
