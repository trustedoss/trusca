# Session Handoff — 2026-05-25 — v2.3 전체 완료 (공급망 무결성 & 우선순위화)

## 1. 무엇을 했나
- **v2.3 마일스톤 전체 완료 (8 PR #165–#172 머지, main CI green).** s-트랙(서명/무결성) ‖ r-트랙(reachability/우선순위화)을 도메인 분리 worktree로 병렬 개발 → 순차 머지. 각 보안 민감 PR은 Producer-Reviewer(security-reviewer) fix-first 통과.
  - **s-트랙 (SBOM 서명 무결성):**
    - #166 **s1** cosign SBOM 서명 — `integrations/cosign.py`(`sign_blob`/`verify_blob`, 고정 argv·`--`센티넬·blob symlink 거부·best-effort skip), cdxgen SBOM persist 직후 `sign` 스테이지(30%) → ScanArtifact 새 kind `sbom_cyclonedx_sig`/(keyless)`sbom_cyclonedx_cert`(**마이그 없음**, 기존 `kind`+미사용 `sha256` 재사용). **D2 ✅ key-based 기본 + keyless(OIDC) 옵션**: private key password Fernet(`core.crypto`, `GITHUB_APP_ENCRYPTION_KEY`) 암호화 → `COSIGN_PASSWORD` subprocess env로만(argv/로그 금지, prod fail-closed). `scrubbed_env_for_cosign`(Sigstore 엔드포인트만). `Dockerfile.worker` cosign 2.4.1 + **per-arch SHA256 in-repo ARG 핀**. security-reviewer PASS(Crit/High 0)→fix-first(SHA256 in-repo 핀·keyless cert 누락 시 skip[`_sign_keyless`도]·prod fail-closed `SecretEncryptionError` 흡수·stderr 시크릿 스크럽·blob symlink 거부).
    - #167 **s2** in-toto attestation + SLSA provenance — `integrations/attestation.py`(SLSA provenance v1 predicate/statement 순수 빌더 + `cisa_minimum_elements_present`), `cosign attest_blob`(s1 헬퍼 재사용). 서명 성공 시에만 `_attest_sbom` → ScanArtifact `sbom_attestation`/(keyless)`sbom_attest_cert`(마이그 없음). predicate=opaque scan/project UUID+builder+timestamp만(git URL/경로/시크릿 없음). CISA generation-context 강제, NTIA는 SBOM 본문. PASS→fix-first(keyless cert skip·로그 마스킹·subject.name trust 주석). materials(source_tarball_sha256)는 스테이지 순서상 보류.
    - #170 **s3-be** 서명 다운로드 — 기존 sbom 라우터 확장 5+1 엔드포인트(`/sbom/signature`·`/certificate`·`/attestation`·`/attestation-certificate`·`/public-key`·`/signature-bundle` zip[SBOM+.sig+cert|pubkey+attestation(+attest cert)+`VERIFY.md`]), 최신 succeeded 스캔(export와 일치). `services/sbom_signature.py`. PASS(Crit/High 0)→fix-first(**Medium**: public-key PEM 헤더 가드[private key→logged 404]·크기 캡 413·keyless attest cert 번들 포함·VERIFY.md sanitize). 경로 traversal `is_relative_to` 봉인·IDOR 404.
    - #171 **s3-fe** 다운로드 UI — SBOM 탭 "Signature & Verification" 섹션, 주 버튼 signature-bundle(.zip self-contained) + 보조 개별, 404 graceful(미서명/keyless-전용), 검증 가이드 링크(`/docs/reference/sbom-signature-verification`), EN/KO, vitest 7.
    - #172 **s3-doc** 검증 문서 — `cosign verify-blob` 외부 검증 가이드 EN/KO(`docs-site/docs/reference/sbom-signature-verification.md` + i18n/ko), sidebars 등록, glossary(cosign/Sigstore/attestation), build green. 엔드포인트/파일명/명령을 백엔드 소스와 대조.
  - **r-트랙 (Reachability 우선순위화):**
    - #165 **r1** govulncheck Celery — `integrations/govulncheck.py`(subprocess + 스트리밍 JSON 파서, 적대적 출력 방어), `tasks/scan_reachability.py`(규칙3 동기 금지, 보존 소스 tarball, best-effort skip). **마이그 0022**(expand): `vulnerability_findings`에 nullable `reachable`(tri-state)·`reachability_source`·`reachability_analyzed_at`. source 스캔 성공 후 비블로킹 chain(`enqueue_reachability`, `REACHABILITY_ENABLED`). GO-id+CVE/GHSA alias→`pkg:golang/%` finding per-pk UPDATE(멱등). `govulncheck@v1.1.4` 핀(Go 1.25.10 기존).
    - #168 **r2-be** reachability API·게이트 — **마이그 0023**(partial index `WHERE reachable IS TRUE`), `?reachable=true|false|unknown` 필터·`sort=reachable`(reachable→NULL→false, 버킷 내 severity desc), 응답 노출, 게이트 `reachable_critical_cve_count`+`reachable_gate_enforced`. 완화 플래그 `GATE_REACHABLE_CRITICAL_ONLY`(opt-in 기본 off). PASS(Crit/High 0, Medium 2)→fix-first: **safe-by-default fallback**(`analysed>0`일 때만 완화, `blocking=total-unreachable`로 `reachable IS FALSE`만 제외·**NULL 보수적 차단 유지** → non-Go silent-disable foot-gun 제거) + 완화 발동 WARNING + .env 문서 + SCA 코멘트 advisory.
    - #169 **r2-fe** UI 배지 — `ReachabilityBadge`(tri-state: reachable 강조/unreachable muted/null list생략·drawer명시), 인라인 필터·`sort=reachable`·URL 미러링, EN/KO i18n(복수형 미사용), vitest 14.
- **트래커**: 매 머지 직후 갱신. §0 대시보드 v2.3 5/6 🟢, §7 게이트 v2.3 ✅ 충족 기록.

## 2. 결정 사항 / 변경된 가정
- **D2 ✅ 결정: cosign key-based 기본 + keyless(OIDC) 옵션** — 셀프호스팅/온프렘/에어갭 1순위. keyless는 `COSIGN_KEYLESS=true` 옵션.
- **마이그레이션 체인 head = 0023** (0021→0022 reachability→0023 reachable partial index). s1·s2·s3는 마이그 없음(ScanArtifact 기존 `kind`+`sha256` 재사용).
- **GATE_REACHABLE_CRITICAL_ONLY safe-by-default**: opt-in 기본 off. on이어도 스캔의 critical 중 `reachable IS NOT NULL`(분석됨)이 0이면 완화 미적용·전체 차단. 완화 시도 `reachable IS FALSE`만 제외, `NULL`(미분석)은 차단 유지. non-Go(reachable 영구 NULL) 게이트 무력화 차단.
- **병렬→순차 통합 패턴 재확인**: 공유 충돌점 = `Dockerfile.worker`(s1·r1, fenced BEGIN/END 블록으로 자동머지 성공), `.env.example`(다중 PR, 다른 섹션 자동머지), openapi 스냅샷. main.py·api/v1/__init__는 안 건드림(기존 라우터 확장·새 라우터 회피).
- **로컬 main이 s3-doc worktree HEAD(`f318140`)를 따라간 비정상 발생** → `gh pr merge` 후 로컬 main ff 실패(diverge). 원인 불명(worktree base 공유 + 어느 git 작업이 메인 main 포인터 이동 추정). 복구: 사용자 `!git reset --hard origin/main`(로컬 고유 커밋 없음·working tree clean 확인 후, 손실 0). **재발 주의 — worktree 작업 중 메인 레포 main 포인터 점검.**

## 3. 현재 상태
- `main` HEAD = `db6c2c3`. 미머지 PR 없음. main CI green. 마이그 head **0023**.
- **v2.1·v2.2·v2.3 전체 완료.** v2.3 게이트 ✅(서명 SBOM 외부 cosign verify + ≥1 언어[Go] reachability 노출 + security-reviewer green + main CI green).

## 4. 다음 세션이 할 일 (우선순위)
1. **[선택] r3 — 차기 언어 reachability 확대** (종료조건 ≥1 언어=Go 초과분). Go govulncheck는 call-graph reachability가 독보적; Python/JS는 동등 수준 도구 부족(OSV-Scanner 일부·commercial). 베스트에포트 한계 평가 후 착수.
2. **[후속/CI 안정화]** 태스크#8 MultiFernet 키회전(b3 GitHub App + s1 cosign password가 단일 Fernet 공유 `GITHUB_APP_ENCRYPTION_KEY` — 회전 시 brick, GA 전) · 태스크#10 사전존재 CI flake(`uq_*`/`updated_at` dirty-DB 격리, disk-guard 95% 트립) · `docs-site/static/openapi.json` 사전존재 drift(FastAPI 버전·v2.2 catch-up) 정리.
3. **[운영 레인, 사용자]** O1 v2.0.1 이미지 게시(공개 차단점) · O2 데모 GCP 배포 · O3 Helm ArtifactHub · **O4 스크린샷 갱신(서명 다운로드·reachability 배지 UI 반영, EN/KO)**.
4. **[비전]** post-GA 로드맵(v2.0.1/v2.1/v2.2/v2.3) 소진. 추가 마일스톤은 사용자 결정.

## 5. 주의 · 블로커
- **stray worktree/브랜치 정리 보류(무해):** destructive 게이트로 차단. 이번 세션 worktree(`.claude/worktrees/agent-*`: s1/r1/s2/r2-be/r2-fe/s3-be/s3-fe/s3-doc + fix 에이전트들) + 머지된 로컬 feat 브랜치. 사용자 `!git worktree prune` + `!git branch -D feat/v2.3-*` 또는 하네스 GC 대기.
- **CI SAST(semgrep/bandit)·e2e·visual-regression `if: false` 유지** — 보안 민감 PR은 security-reviewer 수동 패스 보완. 배포 전 재활성화 필요(사전존재).
- **자율 머지 시 라이브 `gh pr checks` 재확인** 유지(watch 조기 exit race). squash 머지 시 concurrency로 직전 main run cancelled 정상 — 최신 run 확인.
- **로컬 main = worktree HEAD 따라감 재발 주의**(§2). worktree 병렬 후 메인 main 포인터 점검.

## 6. 다음 세션 시작 지시문 (복붙용)
> TrustedOSS Portal post-GA. main=`db6c2c3`(CI green, 마이그 head 0023). `docs/post-ga-execution-tracker.md`가 PR단위 단일 진실 — **v2.1·v2.2·v2.3 전체 완료**(§0 대시보드·§7 게이트 참조). 잔여=선택: **r3**(차기 언어 reachability, Go 외 도구 한계 평가) · **후속**(태스크#8 MultiFernet 키회전·#10 CI flake·openapi.json drift 정리) · **운영 레인**(O1 이미지·O2 GCP·O3 ArtifactHub·O4 서명/reachability 스크린샷, 사용자). **에이전트 프롬프트 강제:** worktree 상대경로만·시작 시 `git rev-parse --show-toplevel` 확인 / 보고 전 `python3 -m mypy .` 전체(테스트 포함) + `ruff check .` + DB의존 테스트는 fresh 빈 DB(`toss_test_*`, DATABASE_URL만·OWNER/APP export 금지) / 마이그 필요 시 0024부터, 병렬 시 머지 순서 제어 / 신규 엔드포인트는 main.py·api/v1/__init__·openapi 스냅샷 공유(가급적 기존 라우터 확장) / 서명·키·게이트 PR은 security-reviewer 필수 / 자율 머지 전 라이브 `gh pr checks` 재확인. fix-first amend는 force-push 일괄 승인됨(세션 정책). 새 비전/마일스톤은 사용자와 합의 후 착수.
