# Session Handoff — 2026-05-24 — v2.1 전체 완료 + v2.2 a-트랙(리메디에이션 추천) + 문서동기화

## 1. 무엇을 했나
- **v2.1~v2.3 실행 트래커 수립**: `docs/post-ga-execution-tracker.md`(PR단위 single source of truth, 흐지부지 방지 backbone). `post-ga-roadmap.md`(why) ↔ 트래커(how/when/status) 분리.
- **v2.1 전체 완료 (8 PR, 전부 main 머지 + main CI green)**:
  - Track A (VEX 소비/트리아지): #145 export · #148 import(소비) · #150 UI. A2는 security-reviewer Producer-Reviewer(Medium NUL sanitize·Low ×2 fix-first→회귀 PASS).
  - Track B (평가·배포): #146 `/health/ready` · #147 eval 경량 프로파일(`docker-compose.eval.yml`+`scripts/eval-up.sh`) · #149 Helm 프로덕션화(번들/외부 PG·Redis·ingress TLS·migration Job·OCI `chart-release.yml`, chart 0.2.0) · #151 API 레퍼런스(redocusaurus `/reference/api`+`scripts/dump_openapi.py`) · #152 라이브 데모 read-only(`DEMO_READ_ONLY` 미들웨어)+일일 리셋(terraform demo_reset). B5도 security-reviewer fix-first(M-1 OAuth 데모차단·M-2 비번 평문로그 제거·L-3 membership-scoped reset→회귀 PASS).
- **v2.2 a-트랙 완료 (3 PR)**: #153 `fixed_version` 실데이터화(마이그 0017) · #154 의존성 그래프+depth(`component_dependency_edges`+`scan_components.depth`, 마이그 0018) · #156 업그레이드 추천 엔진+UI(semver 최소 안전 업그레이드, 드로어·게이트 코멘트).
- **문서 교차 동기화 (#155)**: ROADMAP(v2.1 shipped)·comparison(EPSS/VEX Shipped)·README·main.py OpenAPI 메타(`version=2.2.0-dev`)·intro·sidebar·CHANGELOG·post-ga-roadmap + 신규 `docs-site/docs/installation/helm.md`(EN/KO).
- **정리**: 원격 머지 `feat/v2.1-*` 8개 삭제.

## 2. 결정 사항 / 변경된 가정
- **D1 = GitHub App** (v2.2-b 자동 PR 쓰기 연동; PAT 아님). 설치형·per-repo 세밀권한·설치별 단기토큰. 2.2-b1에서 App 자격 모델 구현 + security-reviewer.
- **데모 호스팅 = 기존 GCP terraform 재사용** (Hetzner 아님). read-only 모드+일일리셋만 추가.
- **VEX export 부재 정정**: `vulnerabilities.py:221`은 export가 아니라 status 전이 PATCH였음 → A트랙이 export+import 둘 다 구현.
- **OpenAPI 신선도 게이트**: byte-exact `--check`는 Python 버전 간 취약 → 제거하고 docs.yml 빌드 시 재생성으로 전환(#151 follow-up).

## 3. 현재 상태
- `main` HEAD = `7db9a89` (#155). 미머지 PR 없음. main CI green(7db9a89 확정 진행 — backend test ~11분).
- v2.1 종료 게이트 ✅ 충족(트래커 §7). v2.2: a-트랙(3/10)+문서동기화 완료. **b-트랙(GitHub App 자동 PR)·c-트랙(동적 라이선스 정책 엔진) 미착수.**
- 마이그레이션 head = 0018.

## 4. 다음 세션이 할 일 (우선순위)
1. **[v2.2-b] GitHub App 자동 PR** — b1 App 자격 모델(암호화 저장, security-reviewer) → b2 생태계 어댑터(npm 우선)+dry-run → b3 옵트인 PR 생성(security-reviewer). D1=App 확정됨.
2. **[v2.2-c] 동적 라이선스 정책 엔진** — c1 per-team/org 정책 모델(db-designer) → c2 게이트 동적평가(SPDX adversarial) → c3 Admin UI → c4 카탈로그 보강. (c는 a3 뒤 b와 병렬 가능.)
3. **[운영 레인, 사용자 작업]** O1 v2.0.1 이미지 게시(공개 차단점) · O2 데모 GCP 배포(terraform apply) · O3 Helm 차트 OCI 게시+ArtifactHub 등록.
4. **[정리]** 메인 체크아웃 untracked 5개(`health.py`·`readiness.py`·`test_readiness.py`·`0017_*`·`test_fixed_version_extraction.py`)는 `!rm`로 제거 가능(무해). 하네스-locked agent worktree 다수는 하네스가 GC.

## 5. 주의 · 블로커
- **worktree 격리 에이전트 경로 누수**: 일부 에이전트가 메인 체크아웃 절대경로로 써서 오염(A1·A2-fix·2.2-a1). **프롬프트에 "상대경로만, 메인 절대경로 금지, 시작 시 toplevel 확인" 강제하면 차단됨**(a2·a3·b4·b5·doc-sync는 무오염). 누수 시 `git restore`(추적)·`!rm`(미추적)로 복구. → [[feedback-parallel-agent-worktree-shared-files]]
- **공유 파일 충돌**: 신규 엔드포인트 PR들이 `main.py`/`__init__.py`/`openapi_endpoints.json` 공유 → 병렬 시 머지 순서 제어 + 스냅샷 union 검증 필요. v2.2-a/문서동기화는 파일 분리로 무충돌.
- **rm/`git clean -f`/`git push --delete` 등 파괴적 명령**: rm·git clean은 권한 게이트(차단)나 push --delete는 통과. stray 파일 제거는 사용자 `!rm` 필요.
- **사전존재 flaky 테스트 2건**(내 변경 무관): `test_component_detail...`(uq_components_purl cross-suite 격리), `test_update_status_happy_path`(updated_at 타임스탬프). dirty dev DB에서만 실패, 깨끗하면 통과. CI(깨끗한 DB)는 green.
- **자율 머지 시 main CI 확인** 원칙 유지 → [[feedback-autonomous-merge-ci-check]].

## 6. 다음 세션 시작 지시문 (복붙용)
> TrustedOSS Portal post-GA 진행. main=#155(7db9a89, CI green). `docs/post-ga-execution-tracker.md`가 PR단위 단일 진실 — v2.1 전체(8 PR)+v2.2 a-트랙(추천, #153/#154/#156)+문서동기화(#155) 완료. **다음: v2.2-b1 + v2.2-c1을 병렬로 착수**(도메인 분리 — b=GitHub 연동/쓰기, c=정책/게이트). b1=GitHub App 자격 모델(app id/private key **암호화 저장**, 설치별 단기 토큰, per-project 옵트인) — 쓰기 경로라 **Producer-Reviewer(security-reviewer) 필수**, D1=GitHub App 확정. c1=per-team/org 라이선스 정책 모델(허용/조건부/금지+예외+SPDX expression 룰)+마이그레이션(현재 head 0018). 이후 b는 b2(npm 어댑터+dry-run)→b3(옵트인 PR 생성, security-reviewer), c는 c2(게이트 동적평가+SPDX adversarial)→c3(Admin UI)→c4(카탈로그) 순. **에이전트 프롬프트에 "worktree 상대경로만, 메인 절대경로 금지, 시작 시 `git rev-parse --show-toplevel`로 worktree 확인" 강제**(경로 누수 방지). 신규 엔드포인트는 `main.py`/`__init__.py`/`openapi_endpoints.json` 공유하니 병렬 시 머지 순서 제어. 자율 머지 시 main CI 확인. 운영 레인 O1(이미지게시·공개차단점)/O2(데모배포)/O3(차트 ArtifactHub)은 사용자 작업 대기.
