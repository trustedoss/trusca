# Session Handoff — 2026-05-25 — v2.2 전체 완료 (리메디에이션 + 정책)

## 1. 무엇을 했나
- **v2.2 마일스톤 코드 완성 (10/10 PR 머지, main CI green).** 시작 시 main=#155(7db9a89)+a-트랙(#153/154/156)만; 이번 세션에 b·c 전 트랙 + b3-UI를 병렬 실행해 마감.
  - **b-트랙 (GitHub App 자동 PR):**
    - #157 **b1** GitHub App 자격 모델 — `core/crypto.py` Fernet 암호화저장(prod fail-closed), `github_app_credentials`+`github_app_installations`(마이그 0019), 설치별 단기 RS256 App JWT→installation token(`mint_installation_token`), per-project 옵트인. security-reviewer PASS-WITH-FOLLOWUPS(Crit/High 0)→Medium 3(redirect-disable·GITHUB_API_URL allowlist·installation_id 재검증)+Low 2 fix-first(`70eb877`).
    - #159 **b2** npm 어댑터+dry-run — operator-preserving semver 재작성·format-preserving·lockfile 재생성 플래그, `compute_npm_dry_run`(a3 추천 + preserved-source/override manifest), `POST .../remediation/npm/dry-run`. compute-only(마이그 없음). package.json adversarial 하드닝.
    - #160 **b3 백엔드** opt-in 자동 PR — `remediation_pull_requests`(마이그 0021), 타깃 repo는 저장된 설치링크에서만 도출(caller 지정 불가), b1 토큰→branch/commit/PR, change_fingerprint idempotency(creating-as-lock). security-reviewer CHANGES REQUESTED(High `base_branch` 인젝션+Medium 3)→fix-first(`967900a`: base_branch 검증·stale-creating reclaim·패키지명 escape·installation_id 재검증·PR본문 cap).
    - #163 **b3 프론트** Remediation 탭(프로젝트 상세) — dry-run 미리보기·PR 생성(team_admin·opt-in 게이팅)·PR 목록, 신규 `remediation` i18n ns, vitest 10신규.
  - **c-트랙 (동적 라이선스 정책):**
    - #158 **c1** per-team/org 정책 모델 — `license_policies`(마이그 0020, category_overrides·license_exceptions 시한부 waiver·unknown posture·compound 전략·enabled), CRUD API, `get_effective_policy` 우선순위 resolver. (b1과 병렬개발→통합 시 마이그 0019→0020 재번호.)
    - #161 **c2** 동적 게이트 — `services/license_expression.py` 하드닝 compound-SPDX 평가기(길이4096/깊이64/토큰1024 bound, 선형 lexer+depth-guarded recursive descent, un-parseable→unknown posture, never hang/raise/500), `policy_gate` 정책-aware(정책 시 동적 재분류·무정책 시 **byte-identical**, golden 25 통과). 마이그/엔드포인트 없음.
    - #162 **c3** 정책 편집 Admin UI — `/policies` 라우트+사이드바, scope-aware(super_admin=org+팀, 비-team_admin graceful read-only), 신규 `policies` i18n ns, vitest 26신규.
    - #164 **c4** 의무 카탈로그 — `services/obligation_catalog.py`(32개 라이선스 구조화 의무), `sync_catalog_obligations` 멱등 upsert(read-path, 기존 테이블, 마이그 없음). **실제 스캔이 License는 만들되 Obligation 0개였던 갭 해소.**
- **세션 시작 정리:** 로컬 main을 origin(#155)으로 동기화, 미커밋이던 SoT 트래커+핸드오프 3건을 main 보존 커밋(`350d4df`).
- **트래커**: 매 머지 직후 갱신. §7 v2.2 게이트 ✅ 충족 기록.

## 2. 결정 사항 / 변경된 가정
- **마이그레이션 체인 현재 head = 0021** (0018→0019 github_app→0020 license_policy→0021 remediation_pr). b2·c2·c4는 마이그 없음.
- **b1 link/암호화 키:** MultiFernet 롤링 회전은 미구현(단일 Fernet, 회전 시 기존 자격 brick) — b3 GA 전 후속(태스크#8).
- **c4 obligation link:** reference_url 없으면 canonical `https://spdx.org/licenses/<id>.html` fallback(스캔 생성 License는 reference_url이 NULL이 잦음). 통합 시 에이전트의 모순 테스트 2개(link=URL vs link=None, dirty-DB서 동반통과)를 fresh-DB 재현으로 발견·수정.
- **병렬→순차 통합 패턴 확립:** 도메인 분리 PR은 worktree 병렬 개발, 머지는 순차(선행 머지→후행 rebase/cherry-pick+마이그 재번호+openapi union 재생성). 공유파일(main.py·api/v1/__init__·openapi_endpoints.json·sidebars.ts·alembic 체인)만 충돌점.

## 3. 현재 상태
- `main` HEAD = `78e6d2a`. 미머지 PR 없음. main CI green. 마이그 head 0021.
- **v2.2 마일스톤 ✅ 충족** (트래커 §7). v2.1·v2.2 종료. **다음 = v2.3 (공급망 무결성 & 우선순위화).**

## 4. 다음 세션이 할 일 (우선순위)
1. **[v2.3] 공급망 무결성 ‖ Reachability** (순차 트랙 2개, 병렬 가능):
   - s1 SBOM 서명(cosign, **D2 결정 필요**: key-based 기본 vs keyless OIDC — 셀프호스팅상 key-based 권고)·rev✅ → s2 in-toto/SLSA attestation → s3 서명 다운로드 UX+검증문서.
   - r1 Reachability 스캔(Go govulncheck 우선, Celery)→ r2 정렬·게이트·UI 배지 → r3 언어 확대.
2. **[후속/CI 안정화]** 태스크#8 MultiFernet 키회전(b3 GA 전) · 태스크#10 사전존재 CI flake 조사(`test_tampered_signature_rejected` state-leak + `uq_components_purl`/`uq_licenses_spdx_id`/`updated_at` dirty-DB 격리).
3. **[운영 레인, 사용자]** O1 v2.0.1 이미지 게시(공개 차단점) · O2 데모 GCP 배포 · O3 Helm 차트 ArtifactHub.

## 5. 주의 · 블로커
- **stray 정리 보류(무해):** destructive 명령(`rm`·`git worktree remove`·`git branch -D`)이 권한 게이트로 차단됨. 머지된 로컬 feat 브랜치(`feat/v2.2-*`) + 하네스 worktree(`.claude/worktrees/agent-*`) + 내 통합 worktree(`/private/tmp/toss-*-integ`, b2/c2/c4/c4v) 잔존. 사용자가 `!git worktree prune` + `!git branch -D` + `!rm -rf /tmp/toss-*-integ`로 정리, 또는 하네스 GC 대기. b3 doc 경로누수 stray는 b3 머지로 추적본화되어 해소됨.
- **CI SAST(semgrep/bandit)·e2e·visual-regression이 `if: false`(TEMP-DISABLED-CI "re-enable before deploy")** — 보안 민감 PR은 security-reviewer 수동 패스로 보완 중. 배포 전 재활성화 필요(사전존재).
- **"에이전트 green ≠ CI green" 2종:** (a) 에이전트가 `mypy`를 소스모듈만 타깃→테스트파일 누락(c1 #158 typecheck CI 실패). (b) dirty 로컬 DB가 격리버그 가림(c4 #164). **대응:** 에이전트 프롬프트에 `python3 -m mypy .` 전체 + DB의존 테스트는 fresh DB 강제. 통합 시 오케스트레이터가 fresh `toss_test_*v` 재현. → 메모리 [[feedback-agent-full-mypy-not-targeted]].
- **자율 머지 시 main CI 확인** 유지. `gh pr checks --watch`는 새 런 등록 직전 조기 exit 0 race 있음 → 머지 전 라이브 `gh pr checks` 재확인.

## 6. 다음 세션 시작 지시문 (복붙용)
> TrustedOSS Portal post-GA. main=`78e6d2a`(CI green, 마이그 head 0021). `docs/post-ga-execution-tracker.md`가 PR단위 단일 진실 — **v2.1·v2.2 전체 완료**(v2.2 b/c 트랙 #157–#164 + a-트랙). §0 대시보드·§7 게이트 참조. **다음 = v2.3 (공급망 무결성 & 우선순위화).** s-트랙(s1 cosign 서명 **rev✅·D2 결정 필요**→s2 in-toto/SLSA→s3 UX) ‖ r-트랙(r1 govulncheck Celery→r2 게이트/UI→r3 언어확대)을 도메인 분리로 병렬 가능. **에이전트 프롬프트 강제:** worktree 상대경로만·메인 절대경로 금지·시작 시 `git rev-parse --show-toplevel` 확인 / 보고 전 `python3 -m mypy .` 전체(테스트 포함) + `ruff check .` + DB의존 테스트는 fresh 빈 DB(`toss_test_*`)에서 / DATABASE_URL만(OWNER/APP export 금지). 마이그 필요 PR은 0022부터, 병렬 시 머지 순서 제어(선행 머지→후행 재번호). 신규 엔드포인트는 main.py·api/v1/__init__·openapi_endpoints.json·sidebars.ts 공유. 자율 머지 시 머지 전 라이브 `gh pr checks` 재확인. 보안/서명 키 PR은 security-reviewer 필수. 후속: 태스크#8 MultiFernet·#10 CI flake. 운영 레인 O1/O2/O3 사용자 대기.
