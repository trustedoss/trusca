-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
# Pinned because ci.yml's coverage-gate installs the same version to read the
# data files these legs write — `coverage combine` needs both sides to agree.
coverage[toml]==7.15.0
# Scores the lines a pull request changed against coverage.xml (CLAUDE.md §2's
# "new code >= 80%"). CI-only; nothing in the runtime image imports it.
diff-cover==10.4.1
ruff==0.8.4
mypy==1.13.0
# Phase 2 PR #8: in-memory Redis double for CircuitBreaker unit tests.
# fakeredis ships under BSD-3 (Apache-2.0 compatible). The [lua] extra pulls
# in lupa so EVAL works against the breaker's _PROBE_GATE_LUA contract.
fakeredis[lua]==2.26.1
# W8: parses `helm template`'s rendered YAML (PodDisruptionBudget,
# topologySpreadConstraints) in the chart render-golden tests. Test-only;
# nothing in the runtime image imports it. types-PyYAML is the stub package
# mypy needs (PyYAML ships no inline types).
PyYAML==6.0.3
types-PyYAML==6.0.12.20260724
