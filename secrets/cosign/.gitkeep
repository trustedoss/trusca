# Placeholder so the default cosign key mount path (COSIGN_KEYS_HOST_PATH=./secrets/cosign)
# resolves on a fresh checkout. Real keys (cosign.key/cosign.pub) are gitignored.
# Generate them with: bash scripts/cosign-keygen.sh
