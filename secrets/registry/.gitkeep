# Placeholder so the default private-registry auth mount path
# (REGISTRY_CONFIG_HOST_PATH=./secrets/registry) resolves on a fresh checkout.
# Put whichever of settings.xml / .npmrc / pip.conf / .netrc you need here —
# all are gitignored. See .env.example's "private registry auth for
# source-scan dependency resolution (#400)" section and the user guide.
